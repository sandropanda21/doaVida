import { StyleSheet } from "react-native";

export const headerButton = StyleSheet.create({
  headerButton: {
    justifyContent: "center",
    alignItems: "center",
    height: 40,
    width: 40,
    borderRadius: "50%",
    backgroundColor: "#F3F4F6",
    marginRight: 16,
  },
});
